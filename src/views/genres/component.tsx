import { Box } from '@mui/material';

const Genres = () => {
    return (
        <Box sx={{paddingTop: '2.5cm', paddingLeft: '1cm'}}>
            <div>GENRES</div>
        </Box>
    );
}

export default Genres;