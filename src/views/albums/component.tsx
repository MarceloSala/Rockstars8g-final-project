import { Box } from '@mui/material';

const Albums = () => {
    return (
        <Box sx={{paddingTop: '2.5cm', paddingLeft: '1cm'}}>
            <div>ALBUM</div>
        </Box>
    );
}

export default Albums;